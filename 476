class Solution(object):
    k = int(1)
    def findComplement(self, num):
        h = num
        while (num > 1):  
                self.k = self.k + 1
                num = num / 2 
        return pow(2,self.k)-h-1